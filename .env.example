# Example environment variables
OPENAI_API_KEY=
LANGCHAIN_API_KEY=
LANGCHAIN_TRACING_V2=true
# Any other provider keys used for vision/llm
VISION_API_KEY=
# Example environment variables (do NOT commit real keys)
# Provider API keys used by LLM/vision integrations. Fill these from your
# account dashboards. Keep secrets out of the repo.
OPENAI_API_KEY=
LANGCHAIN_API_KEY=

# Enable LangChain/LangGraph tracing for debugging multi-agent flows
# Set to 'true' to enable LangSmith/LangChain tracing (if configured).
LANGCHAIN_TRACING_V2=true

# Optional vision / multimodal provider key (if using VisionInspector)
VISION_API_KEY=

# Runtime configuration
# Set the Python version your project targets (used by CI / Dockerfiles):
PYTHON_VERSION=3.8

# Git credentials: if you need to interact with private repos, configure
# a personal access token instead of embedding passwords. Example usage:
# GIT_AUTH_TOKEN=ghp_xxx
GIT_AUTH_TOKEN=
